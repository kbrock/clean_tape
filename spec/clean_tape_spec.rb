require 'spec_helper'

describe CleanTape do
  it 'has a version number' do
    expect(CleanTape::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
